#ifndef HUD_H
#define HUD_H

#include <stdint.h>

/**
 * Draw the HUD (heads-up display) showing player score, enemy score, 
 * game score, and level information
 */
void draw_hud(void);

#endif // HUD_H
